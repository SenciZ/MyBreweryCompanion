import styled from "styled-components";

export const HamburgerIconContainer = styled.span`
    display: inline;
    background: red;

    .hamburger {
        color: white;
        background: white;
    }
`;